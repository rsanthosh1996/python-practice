sets: to store collection of unique elements 
{}

doesnt allow duplicate items only unique elements
it maintains unordered collection.it doesnt follow insertion order

empty set x = {} doesnt work  it ll consider it as dict

emptyset  x = set()
non empty set x = set([1,2])or x= {10,20}

its mutable ...and not subcriptable  no indexing is possible since order keeps changing.using loops we can retrive elements.
print(s[0])

 x = {1,3,3,5,6,7,7}
print(x) gives = {1, 3, 5, 6, 7}

predefined  functions :

x = {"python","java","Javascript"}
y = {"c","dotnet","ruby"}

add(item) : since no index so we  use item
x.add("goat") give  {"python","java","Javascript","goat"} memo  loca doesnt change so  its mutable

x= "santosh"
x = "fun"
 
 First line:
A set object is created in memory
x points to it → say ID = 140245678901456
Second assignment:
A new set object is created → ID = 140245678902112
x now points to this new object
❓ What about the first object?

Yes—it becomes unreachable, because:

No variable is pointing to it anymore
So it is eligible for garbage collection

x = {"santosh"}
y = x

No new memory location is created, x and y point to same location and set.


print(id(x))
print(id(y))

👉 Now both IDs will be same, because y points to the same object.


x = "santosh"
x = "santosh" 

here also same location.

shallow copy 

x.copy()  will  create a copy in diff location

x.clear() will empty set

x.difference(y)  gives set having the diff between two sets gives first  set  differnce here its x (only x)
y.difference(x)  elements in  x same as y are moved  and given elements from y (only y)


x = {"santosh","sam"}
y = {"dev","santosh"}

z=x.difference_update(y)
print(z)
print(x)
print(y)

removes common elements from x and y remains same


A = {1, 2, 3, 4}
B = {3, 4}

C = A.difference(B)  == A-B  (ven diagram gives only A )

print(C)   # {1, 2}
print(A)   # {1, 2, 3, 4}  -> original unchanged
difference_update()

Removes elements from the original set itself.

A = {1, 2, 3, 4}
B = {3, 4}

d =A.difference_update(B)

print(A)   # {1, 2} -> original modified


x.intersection(y) it returns common elements 
x.intersection_update(y) it modify  x  and returns none

remove()
x = {"java","python"}
x.remove("django")
gives key error since django is not there in set every element is treated as key 
x.discard("django") doesnt give error it ll just not return anything
Each element in a set is treated like a unique key.
Methods that modify objects directly usually return None.
remove() and discard() both gives None.


x.isdisjoint(y) -  gives True or False True when there is no match keys in both sets else false

subset and superset 
x = {10,20,30,50}
y = {10,20}
x  is superset  of y since it holds all elements of y in it  
and y is subset of x since it holds some elements of x  in it.

y.issubset(x) gives true 
x.issubset(y) gives False
x.issuperset(y) gives true
y.issuperset(x) gives false


Set elements must be immutable/hashable but set is mutable.=A mutable container can still hold immutable items.
There are two different things here:

The set object itself = mutable
The elements inside the set  = immutable (example : tuple,str,int)

Python uses hashing internally
Changing an element would change its hash and break the set structure
s = {1, 2, 3}

# You cannot do:
s[0] = 10   # Error (not subcriptable)

Sets are unordered, so indexing also does not exist.
s = {1, 2, 3}

s.remove(2)
s.add(20)

print(s)

x.symmetric_difference(y) = return new set no modification = A+B
gives only A and only B values in set 

x.symmetric_difference_update(y) = updates  set x with only A and only B .
modifies set x , doesnt return set 

differnce = A-B only unique elements in A
Symetric difference  A^B 

x.union(y) result is returned, same result as update 
x.update(y) result is not returned, same result as union

pop()- removes random element , No index allowed.
Because sets are unordered and not subscriptable.

hash value  is assigned to each element  in set and dict . 
based on hash value set is ordered but no guarantee.

iter(x) argument can be set ,list,tuple,....
iter() function creates aND RETURN WHEN CALLED iterator object NOT ACTUAL VALUE
An iterator lets you access elements one by one.

iter() creates iterator
next() asks iterator for next value


x = {1,2,3,5}
d = iter(x)

print(next(d))
print(next(d))
print(next(d))

Nested Set concept	
sets are not hashable = set is mutable but elements are immutable.
hashing is not possible,hashing objects must be immutable.

hashing - Hashing is a way of converting data into a fixed number called a hash value to make searching very fast.
it is done mainly in Dict and Set, where ever hashing is done and if we are doing nesting ,that must be immutable.Since system takes them as keys 

Not possible
{1,2,3,4,{1,2,3},12}
{1,2,3,[1,3,4],34}

Possible in set- tuple since tuple is immutable
{1,2,3,4,(1,3,4),42}

set inside tuple and list possible since its not dependent on hashing
[1,3,5,{14,5},5]
(32,53,{3,6},5)

Frozen Set
set converting to immutable = frozen set (def:immutable version of set)

Creating Frozen Set
s = {1,2,4,5}
s1 = frozenset(s)

OR s = frozenset({1,2,4,5})
Must give Set as argument in Frozen Set function x or {1,2,3,5,}like above.


functions Accepted where modifications are not there
isdisjoint()
issuperset()
issubset()
union()
intersection()
difference()
symmetric_difference()

set to tuple and reverse  / set to list and reverse

Tuple
Ordered
Immutable
Allows duplicates

Set
Unordered
Mutable
Removes duplicates

set elements are stored as key 

s = {"apple", "banana"}

{
    "apple": True,
    "banana": True
}

Hence its hashable.
 
 
 
Dictionary def = To store data in key value pairs
Same as Set but stored as key value pairs.
key value pairs  are seperated by ,
String " given else not required
 
Creating Dict
user = {"name":"santosh","age":39,"phone":9391958704}

We cant use index to retrive user[0] since , key can also be 0 dict doesnt know difference.
example user = {0 = "santosh",1 = "dog"}

There fore we call by keys
user[name] gives  santhosh
Since we are calling by keys to get values , keys must be unique, similarly  in sets

user = {1:23,1:43}

print(user) gives {1:43}
print(user[1])
print(user[1])
Python keeps only the last value for that key .



dictionary lec -2 
In a Python dictionary:

dict is mutable

Keys → must be hashable (immutable) - hence cant be list,sets.
Values → can be any data type

Creating empty dict

user = {}
user = dict()

Creating non empty Dict

user = {"name":"santosh"}
d = dict(name="Santhosh", age=25) be mind ful here abt syntax

Built  in functions
clear() - it removes all items from  dict.gives {} but maintains same memory location.

c = user.copy() - shallow copy new location , no effect on each other.

get() = same as user[name] ,by using predefined function also we can retrive value by giving key, user.get(name) gives santosh. 
but this function doesnt give key error.

pop(key) = removes that key and value pairs
pop() doesnt work in tuple, pop()removes random item in set .

user.pop("name")  returns santosh and removes pair. syntax give "" for key if its string if integer not required

popitem() - user.popitem() removes last element from dictionary. no argument given.

setdefault(key,value)  - sets default value for a key, 
returns value of specified 	key (if key  exists)
inserts the key with specified value(if key doesnt exists)

user = {"name":"santosh","age":46,1:54}
print(user.setdefault("name","vid")) gives "santosh" doesnt change.
user.setdefault("address","sainikpuri") inserts new pair 
print(user)

update to add new value or update exisiting value

{key:value}) --" if its string OR update(key = value) " not required in key in value give "

adds
user = {"name":"santosh","age" :29}
user.update({"address":"sanikpuri"})
print(user)

update 
user = {"name":"santosh","age" :29}
user.update({"name":"sanikpuri"})
print(user) gives 


keys() -  gives list of all keys.dict_keys([])
values() -  gives list of all keys.dict_values([])
items() - gives list of containing a tuple for each key value pair . [(k1,v1),(k2,v2)]
fromkeys(x,y) -  returns a dictionary with specified keys and values.
x -  tuple of keys , y - value.

x = (1,2,4,5)
y = 0
new=dict.fromkeys(x,y)
print(new)

gives dictionary  {1: 0, 2: 0, 4: 0, 5: 0}


To add dictionaries
user = {"name":"santosh","age" :29}
user_1 = {"name":"taj","age" :49}
user_1.update(user)
gives = {'name': 'santosh', 'age': 29}-- "name" already exists,so its value is replaced

if different key value pair is given then it adds
user = {"name":"santosh","age" :29}
user_1 = {"address":"taj","plot" :49}
user_1.update(user)
print(user_1)

nested dict:
x  = {1:100,2:200,3:300,4:{4:400,5:500},6:600}
print(x[4])

list,tuple,set inside dict

dict inside list
dict inside in tuple
dict inside set not possible - hashable its has to be  immutable.

	


---
bytes, bytearray, memoryview, range,None Data Types

Vedios ,audio ,pics , network communication are stored in raw bytes not in int,string etc
here bytes, bytearray  are used to store raw  bytes.

converting int to bytes
a= 8
c= bytes(a) gives b'\x00' (hexa decimal)

b = 9
d = bytearray(b) gives bytesarray hexa deimal bytearray(b'\x00')

bytes must be range 0- 255 
list_1 = [1,2,3,255]
c = bytes(list_1) -   converts to bytes 
when we retrive back it ll show ,  c[0] it ll show 1.

bytes doesnt support item assignment or modification
bytearray is mutable.
l = [1,2,3,4,5]
p= bytes(l)
q = bytearray(l)

p [0]= 1 -- error immutable
q[0] = 9 --- works mutable 

generally dont .copy() bytes , bytearray files since more memory is taken 
hence we have to modify existing files we use memoryview.

Memory View is used to access and modify byte data w/o copying it.
bytes and bytearray support buffer protocol hence only memory view can be used .

x = memoryview(l)
print(x) - It directly views the same memory buffer used by p.


None Data type =  To represent That there is nothing there.,its not space or empty.

print(a.remove()) == None.

or 

x = None
We can later assign value here.

range Data type

a = range(6)
print(a,type(a))
Internally it stores mainly:
start → 0
stop → 6
step → 1

It creates a special range object that generates numbers only when needed.memo efficient
not list is creatd 
a = list(range(6)) created list

range	                   list
Memory efficient	       Uses more memory
Generates values lazily	   Stores all values
Immutable	                Mutable

x = range(1,10) takes from 1 to 9 
takes only integer data type 

x = range(1,40,3) = 1,4,7,...step range

---
Print Statement
print(a) thinks of a as variable.,goes to memo  loca and prints value there
print('a')
print("a") prints as it is

comma based 
print(a,b) - 10,20
print(a,type(a)) - 10,int

exp= 13
print("my exp  is ",exp) use comma 
print("my exp is "+str(exp)) -- string concat use +

comma -  space adds , + no space  
print("helo"+"santosh") helosantosh
print("hello","santohs") hello santos

x = range(1,7)
for i in x:
  print(x,end =" ")
  
print("-"*50)
------------------------------

format specifiers
%d for int
%f for float
%s for string

a  = 10
b  = 2.5
c = "santosh"

print("value  of a is %d" %a) gives value  of a is 10
print("value  of b is %f" %b) gives value  of a is 2.50000
print("value  of b is %0.2f" %b) gives value  of a is 2.50
print("value  of b,a is %f,%d" %(b,a)) gives value  of b,a is 2.50000,10
print("my name is %s" %c) gives my name is santosh

format function
a = 10
b = 30
print("a value is {} and b value is {}".format(a,b))
a value is 10 and b value is 30

l  = [1,2,45,6]
print("l value is {}".format(l))
print("l value is %s"  %l)

f strings

a = 10
print(f"value of a is {a}")


How to write Program
program  = logical thinking
Know what to do.
step 1 define in simple steps 
step 2 convert to program
step 3 execution

 
Operators - 1

use comments #

How to read values from user/keyboard

input() if we give 10 also it ll  take as string 

a = input()
b = input()
c = a+b
print("vaue of c is ",c) gives 

10
20
vaue of c is  1020

therefore use int(input())

code
print("enter a value  ")
x = int(input())
print("give b value")
y = int(input())

z = x+y

print("value of a+b is",z)

other way to code

x = input("enter your age ")
print(x)


Arthimatic Operators = + , - ,* ,/ , // gives int quotient, % gives remainder , ** exponent
Assignment operators  - =,+= ,-=  ,arith operators =
a = 10
a += 2
a = a+2

a/=4
a=a/4


comparison/relational operators = finding comparison  > < >= <= != ==

logical operators  - To combine more than one condition, And /or /not  
a =10
b =20
c =30

a<b And a<c
True And True = True
Short circuit execution - in AND Condition (100 and 200) gives 200 True and True 
Short circuit execution in OR (10 or 30) gives 10 = True and True 
Base True 

Identity operators = to check two objects  pointing to same memo location, is /isnot 
example

a= [1,2,4]
c = a.copy()
print(a is c) gives false

membership operators  - in / not in  (think student is in membership pool or not)
l = [1,2,3,4]
4 in l = true else false
4 not in l false else true (4 shoud not be in l but its there hence fasle)


bitwise operators - converts first  to bits and oprtators  on bits (hold)

&
|
^
~
<<
>>

operators  -3
 
a = 350
b = 350

difference memo location


a = 10
b =  10
same memo  location

-5 to 256 range values , Registers = small memories for very fast execution.hence 10 stored in registers a ,b points same location.
more than these values values goest to main memory.

Mutable examples:

list
dict
set

Immutable examples:

int
float
str
tuple
bool


a  = "TWG"
b = "TWG" 
a  is b True 

Depends  may be True or False depending on optimization.

- 
value  assignment 
a,b = 10,20 single line assignment
a=b=20 
a,b = 10,10 single line assignment


Python evaluates value once and both variables reference the same object — regardless of:
in single line assignment
data type
size of value
mutable or immutable

operators priority


and has higher priority than or in Python.

So:

True or False and False

is evaluated as:

True or (False and False)

Arithmetic operator precedence in Python:

Priority	Operators
1	()
2	**
3	unary +, unary -
4	*, /, //, %
5	+, -


###control statements 

operators  precedence 
()
x[index],x[begin:end]
**
~
*/ // %
+ -
<< >>
&
^
in , not in ,is ,is not , <  <= > >=
not 
and 
or
:=


(2+3)*5 first here + will go since its in ()

2*3/5 
